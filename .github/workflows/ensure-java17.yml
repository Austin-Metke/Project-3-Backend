name: CI - Java 17.0.6

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

permissions:
  contents: read

jobs:
  build:
    name: Build & Test (Temurin 17.0.6)
    runs-on: ubuntu-latest

    env:
      CI: true
      SPRING_PROFILES_ACTIVE: test  # ensure test-safe config is used

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17.0.6 (Temurin)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17.0.6'
          cache: gradle

      - name: Verify Java version
        run: |
          java -version
          javac -version

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Build & test
        run: ./gradlew clean build --no-daemon

      - name: Publish test report (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            **/build/reports/tests/test/**
            **/build/test-results/test/**
          if-no-files-found: ignore
